# new

🔵 v030-refactor — refactor the code to the RATIFIED v0.3.0 design (architecture.md @ 4cce4be). Eight steps, each shipping WITH its tests, pure-first→wire-last: (1) `tape` ns — extract ⊕ absorb chat-memory whole (compaction band + fold included, destination ≡ compact!) ⊕ full test coverage; (2) `registry` — chokepoints: swap!-only mutations (append > replace, raced→receipt), EDN assert, version counter, events-as-data {:id :at :kind :slug :msg}, wait-for-event!; (3) `completion` — extract build-request/plain/tool loop, the :complete-fn contract as a named layer; (4) api ns `us.whitford.llm-repl` — commands ⊕ ONE grammar ⊕ compact!; core.clj deleted; (5) `client` — version-poll ⊕ long-poll on wait-for-event!, structural suppress-echo (kills the :repl/id regex); (6) `tui.frame`/`tui.term` split ⊕ headless tests ⊕ main rewire; (7) `daemon` JVM spawn fails loud, `roster` dynamic config/real reload (D7); (8) build.clj ⊕ ci.yml/release.yml (fulcro-rad-datalevin model, bb∧JVM twin in CI) → first -alpha local install → anima :local/root. · knowledge/design/architecture.md · knowledge/design/library-contract.md · 4-8 sessions
⚪ tui-local-daemon-human-pass — live terminal verification of `bb llm-repl` over a LOCAL daemon: spawn→attach→detach→daemon survives→reattach sees persisted tapes→bb stop. Every programmatic path verified; the container-attach TUI is human-verified; ONLY the local-daemon TUI render lacks a human pass. · knowledge/attach-topology.md · ~10 min human
⚪ compact-live-trial — self-compaction first exercise once refactor step 4 lands: the model (armed, :tools) calls compact! on its OWN session's aged turns; verify band acceptance, ⚡ receipt, :original retained, prefix-cache impact (one-time bust at the rewritten cell). The one-true-write, exercised by the machine it rewrites. · knowledge/self-eval.md · design § D1 · ~1 session
⚪ tape-persistence — daemon-owned disk snapshots (matters only across daemon restart; daemon already holds tapes across TUI detach). Likely <proj>/.llm-repl/tapes/ EDN at mutation or SIGTERM; the session fold (tape/apply-fold) is the cross-session compression when it lands. Placeholder seam named in design. · knowledge/design/architecture.md § placeholders · 1-2 sessions
⚪ mcp-facade — compile the MCP tool list from (manual): :summary → tool descriptions, :doc → depth. Sits on the api ns only; nothing new to maintain (the manual seam is the source). · knowledge/design/architecture.md § placeholders · 1 session
⚪ bbin-entry — `:bbin/bin` → `llm-repl` on PATH. DEFERRED until feedback says people actually want to install it. · ~30 min
⚪ split-pane-tape-view — watch a second session live (right pane splits). Rides tui.frame purity post-refactor. · knowledge/tui-design-rules.md · 1 session
⚪ compare-pane — ab! children side-by-side with config deltas; rides the SAME overlay slot as help. · knowledge/tui-design-rules.md · 1 session
⚪ bb-status-all — machine-wide daemon list (~/.local/state index keyed by project) if the per-project view proves insufficient. · ~1 session

# complete

✅ v030-design — VERDICT 2026-08-27 meta session: full-codebase assessment (every ns read; zero tests found despite claims — coherence lesson encoded at λ coherence) → v0.3.0 architecture ⊕ library-contract RATIFIED @ 4cce4be. Six layers, api ns ≡ us.whitford.llm-repl, tape absorbs chat-memory → compact!, swap!-discipline, registry chokepoints, events-as-data, formal shape (PTM/oracle/NTM/one-true-write/KV≡head). Mementum bootstrapped same session: state.md 250→~70 lines, 4 knowledge pages, 3 memories, VSM-shaped AGENTS.md, this queue. · knowledge/design/architecture.md
✅ v020-self-eval-release — v0.2.0 tagged ∧ announced (Clojurians Slack): :tools knob, clojure_eval, environment orientation, config machine-fact, README § self-eval. The accrete-as-we-go era's capstone. · knowledge/self-eval.md
