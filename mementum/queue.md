# new

⚪ anima-re-migration — HUMAN follow-up, the ratified step-function's next step (migrate → suggest → ingest → re-migrate): anima updates to (1) D11 qualified session keys — `:us.whitford.llm-repl/*` in its adapter's open!/eval! opts ∧ any snapshot :config reads; (2) D10 inert config — its ambient exposure ends at the next `:local/root` sync automatically, optionally `(roster/init! {:map …})` for its own source; (3) D8 guard errors — `:repl/error` may now be a MAP `{:command :errors :args}` on schema rejects, anything string-reading :repl/error should handle both. llm-repl side DONE; rides anima's repo ∧ the human. · design/library-contract.md § 1 § 6 · ~half session
⚪ open-defaults-create-only — from SUGGEST 2026-08-29: hosts want creation-only defaults, but open! merges opts into an EXISTING session's config too, forcing a snapshot-check-then-open! TOCTOU outside the registry swap (the exact shape D2 removed internally). Add opts key `:defaults` — merged UNDER other opts on the CREATE branch only, inside the mutate! fn (race-free where D2 lives); ignored for an existing session; config-keys semantics untouched. Small build ⊕ tests. · src/main/us/whitford/llm_repl.clj open! · ~1h
⚪ trace-capture-hook — from SUGGEST 2026-08-29: `:io/ref` rides ✓ only when `(nil? (:complete-fn opts))` (llm_repl.clj:430) — honest (the capture seam lives in completion), but a host injecting its backend loses the flight recorder even with trace initialized. EITHER expose `trace/capture-response!` (slug, turn, payload) so a host's complete-fn opts in and :io/ref stamping resumes, OR just document the bypass in library-contract § 3. Decide when touched; anima doesn't need it (escapement session dirs ≡ its evidence tier). · design/library-contract.md § 3 · ~1h
⚪ bbin-entry — `:bbin/bin` → `llm-repl` on PATH. DEFERRED until feedback says people actually want to install it. · ~30 min
⚪ split-pane-tape-view — watch a second session live (right pane splits). Rides tui.frame purity post-refactor. · knowledge/tui-design-rules.md · 1 session
⚪ compare-pane — ab! children side-by-side with config deltas; rides the SAME overlay slot as help. · knowledge/tui-design-rules.md · 1 session
⚪ bb-status-all — machine-wide daemon list (~/.local/state index keyed by project) if the per-project view proves insufficient. · ~1 session
⚪ clock-seam — 9 scattered System/currentTimeMillis, no injectable clock: tests can't control time without redefs. Low priority; do when a test actually needs it. · knowledge/state-audit.md · ~1h
⚪ agent-recipe-page — "driving llm-repl from any agent": connect (bencode/bb one-liner), eval commands, poll events (D3 long-poll). Replaces the MCP facade with documentation — the manual seam feeds a recipe instead of a component. DEPRIORITIZED 2026-08-29: still working through the new design; recipe waits until the surface settles. · knowledge/design/architecture.md § placeholders · ~half session

# complete

✅ manual-malli-schemas — VERDICT 2026-08-29: strictness arc 6/6 — THE TAG GATE IS CLEAR (v0.3.0 tag ≡ human move). D8 BUILT ⊕ **D11 RATIFIED ∧ BUILT same session** (human raised qualification mid-design; the contract break rode the schema birth — ONE migration, not two). D11: the seven session knobs ⊕ ::orientation → `:us.whitford.llm-repl/*` — the session :config ESCAPES via snapshot into host maps, so its keys are collision-proof against ANY keyword; scope pinned: config-file root ∧ :repl/* results ∧ index projection ∧ tape keys stay bare (deferred, human-ruled) · ephemeral opts (:complete-fn :xform :at) bare · api spells ::model, lower layers full literals (keywords need no require — layer-safe), README/config.example.edn teach the ::-shorthand. D8: `guard/errors` pure fn (errors-or-nil polarity; humanized ⊕ the full :args form rides EVERY error — :catn humanize is POSITIONAL, the form carries param names) ⊕ `defcommand` in defn's exact grammar, expansion ≡ hand-written `(or (errors …) (do body))`, macroexpand-1 ≡ spec; two gaps RULED pre-build (human): `:defaults` DECLARED per `[:? ]` param (compact! defaults tape/default-floor, NOT {} — no implicit convention) · `roster/session-opts-schema` ≡ the ONE knob source (config-schema's root never held the session keys; shared shapes derived via malli.util; CLOSED — a typo'd knob TEACHES where select-keys silently dropped). 16 conversions (15 api ⊕ main/use!); unset!'s enum lists the whole unsettable set (its 2 hand-validation branches dead); (manual) exports :args as m/form data (zero-param [:catn] canonicalizes to bare :catn — locked). Coverage ≡ behavioral table-test over (manual) ITSELF: a future command cannot ship unguarded. FOUND EN ROUTE: kondo `:lint-as defn` ≠ enough (one arity ⇒ 26 invalid-arity at defaulting call sites) → macroexpand HOOK mirrors the real expansion ⊕ or-wrap ⊕ emits :args for usage analysis, D8 amended in-build · JVM macroexpand-1 wraps macro throws in CompilerException while bb rethrows BARE → tests match the JOINED cause chain (memories/jvm-macroexpand-wraps-macro-throws). Suite 218/728 → 227/780 bb ∧ JVM twins, kondo clean hermetically, --plain live-smoked (16 commands, generated arglists correct). OWED → ⚪ anima-re-migration. · architecture.md § D8 § D11 · library-contract § 1 § 6

✅ library-config-inert-default — VERDICT 2026-08-29: D10 RATIFIED ∧ BUILT one session — the library is INERT at require, the anima leak structurally dead. `config*` holds `{:source :value}` in ONE atom (require ≡ `{:builtin true}`, zero IO — source ∧ value can never tear); the source is DATA (`{:builtin}|{:map}|{:fn}|{:files}`), every shape folds over builtin-defaults through the ONE validate-config; `init!` throws loud on the caller's stack (D9) and installs NOTHING on failure (locked); re-init REPLACES atomically returning `{:source :replaced}` (ratified: no read-tracking — stickiness law keeps open sessions' materialized configs); `reload-config!` re-folds from the CURRENT source — the reload-resurrects-leak hole locked by test (init! `{:map}` then reload against a poison chain reads nothing); `{:fn}` re-invokes (live), `{:files}` re-reads captured paths (LLM_REPL_CONFIG read once, at chain construction). Forks ruled as recommended: replace-loudly · fold-over-builtins only (`:replace` ≡ YAGNI, the unset!-re-seed law: whatever governs a fresh session governs) · mechanism stays in roster, `init!`/`reload-config!`/`config-sources` PROMOTED to library-contract § 6 (no api re-export — init! is boot, not a session command, so it stays OFF the manual). Entrypoints read the chain EXPLICITLY: main/-main first act ⊕ bb status's requiring-resolve site. Fresh-process inertness probe (bb subprocess, poison LLM_REPL_CONFIG, PATH-guarded) rides the suite — the regression can't return quietly. Suite 210/709 → 218/728 bb ∧ JVM twins, kondo clean; --plain ∧ bb status live-smoked (status showed the container :attach — the explicit read WORKS). Full hermeticity (config as system value) stays NAMED for v2. PRE-RC gate CLOSED. · architecture.md § D10 · library-contract § 6 · memories/ambient-config-leaks-into-embedding-hosts

✅ config-unset-semantics — VERDICT 2026-08-29: strictness arc 5/6, the sticky config's release valve SHIPPED. `unset!` (api ns, `^:manual`, variadic `(unset! slug & ks)`): errors-as-data per D9 (a command's return is READ — unknown key ∨ empty ks ∨ missing session → {:repl/error} that TEACHES `unsettable-keys` ≡ config-keys ⊕ :orientation); mutation inside ONE mutate! fn (D2, race-free), existence detected from `old`; receipt {:kind :unset!}; return shows what now governs the named keys. DESIGN GAP FOUND ∧ RATIFIED IN-BUILD (human, this session): "dissoc → chain resumes" is only literally true for the prompt-stack keys — the five default-SEEDED knobs are materialized at open! and nothing re-reads a chain for them, so bare dissoc MINTS NEW POISON (:model absent ≡ NPE at next send; :preamble? absent ≡ silently falsy; :tools absent ≡ none-not-default). Ratified per-key-class: :system/:preamble/:orientation DISSOC, seeded knobs RE-SEED from the live `(default-config)` (read OUTSIDE the swap fn — pure f) — one mental model: whatever governs a FRESH session governs again. Amendment appended to architecture.md § D7 BEFORE code (λ coherence). nil stays un-overloaded ∧ test-locked: `open! {:system nil}` is preserved (explicitly-none), only unset! removes it. Live probe surfaced the anima-leak in miniature: unset! :tools re-seeded to `true` on this machine — the operator's config governing, the contract working literally (tests assert against `(default-config)`, machine-independent). D8 schema rides the D8 ticket as planned. Suite 206/695 → 210/709 bb ∧ JVM twins, kondo clean. · knowledge/state-audit.md §4 (marked fixed) · architecture.md § D7
✅ registration-guards — VERDICT 2026-08-29: strictness arc 4/6, D9's boundary-idiom rule built at all three seams — every guard throws teaching ex-message ⊕ {:errors …} ex-data (roster's humanize precedent for tone; throws model-safe via tools dispatch → :is-error, host-safe via boot stack, editor-safe via nREPL print). (a) `tools/register-tool!` ≡ the guarded chokepoint over upstream `tp/register!` (whose bare behavior silently REPLACES a live tool): satisfies Tool → keyword tool-name → input-schema COMPILES as malli at registration (else dispatch's gate blows up at CALL time, on the model's turn) → collision throws; `=`-tool re-register ≡ no-op (reload idempotence — the un-ratified nuance, chosen over throw-always because a host reload is not a bug); deliberate replacement ≡ upstream register! directly, the labeled escape hatch. (b) `register-manual-ns!` validates symbol ∧ find-ns with the time-bomb named in the message (typo'd ns 'succeeded' quietly, ns-publics threw in (help) later, far from the cause); nothing lands in the compile set on reject. (c) `event!` got sessions*'s EDN assert via the SAME edn-violations fn — validated BEFORE the ring append (e ≡ a value, so the gate can precede the swap — unlike mutate!'s opaque-f no-rollback pin), ring stays clean ∧ no version bump on reject; split event!* keeps the gate readable. All 13 guard facts bb-probed live BEFORE tests (λ assert). Suite 203/674 → 206/695 bb ∧ JVM twins (guard tests in each seam's OWN test ns, fresh registries — tool-registry* never touched), kondo clean. · knowledge/state-audit.md §5 (marked fixed) · architecture.md § D9
✅ term-state-chokepoint — VERDICT 2026-08-29: strictness arc 3/6. `term/update-state!` ≡ THE TUI-state chokepoint (registry/mutate!'s pattern at TUI scale, audit §2): every result validated against the CLOSED `state-keys` set (12 keys ≡ start!'s seeds ⊕ :overlay ⊕ :events) — unknown key throws loud naming key ∧ allowed set (was: typo'd key ⇒ a flag nobody reads ⇒ silent no-repaint with nothing to debug); same no-rollback pin as mutate! (the atom shows the offending shape). All 13 swap! sites rewired across both namespaces; main.clj's 3 raw reaches became NAMED mutators — `focus-slug!` (use! at any surface) ∧ `set-pending!` (prose-in-flight bracket) — beside the existing show-overlay!/scroll-view! family, so main never touches the atom's shape directly again (the raw assoc was the anomaly, not the style). term.clj gained its FIRST test ns: the chokepoint ∧ named mutators ∧ scroll sign-flip run on a plain atom, no terminal (the byte-moving rest stays out by construction, D5); twin runner wired. Chokepoint bb-probed live before tests (7 facts, λ assert); one test-authoring stumble worth remembering — the no-rollback pin bit its OWN test (second assertion on the same atom saw the first offender still present; fresh atom per throw-case). Suite 200/656 → 203/674 bb ∧ JVM twins, kondo clean, main.clj load-smoked. · knowledge/state-audit.md §2 (marked fixed)
✅ daemon-state-hygiene — VERDICT 2026-08-29: strictness arc 2/6, all four audit-§3 gaps closed in daemon.clj. (1) `write-state!` (public, spawner-owned): spit → temp sibling → `Files/move` ATOMIC_MOVE⊕REPLACE — a concurrent read-state sees old-complete ∨ new-complete, never torn; spawn! now writes through it. (2) `read-state`: corrupt ≢ absent, never silently — unparseable ∨ parseable-but-not-a-map (a torn write can still edn-parse, e.g. `42`) → moved aside VERBATIM to `daemon.edn.corrupt` ⊕ ONE loud stderr line naming both paths, THEN treated absent (clean respawn ⊕ human gets the artifact; the silent version spawned a SECOND daemon racing the first); aside-rename failure is itself named in the line, file left in place. (3) `clean-state!` (now public): returns `{:failed [paths]}` | nil ⊕ a loud stderr line per failed delete — the dropped `.delete` boolean is dead (a survivor file resurrects stale state at the next discover). (4) `read-port-file` ≡ THE one .nrepl-port parse path replacing the 2 divergent ones (attach-target parseInt ∧ spawn!'s double-slurp parseLong): absent/blank → nil, garbage → loud ex-info carrying file ∧ content (was a bare NumberFormatException at both sites). All 11 behavior facts bb-probed live BEFORE tests (λ assert; read-only-dir delete-failure trick works on macOS, test guards on setWritable's own boolean for CI). Public-for-tests per memories/bb-jvm-private-var-twin-trap. Suite 196/634 → 200/656 bb ∧ JVM twins, kondo clean. · knowledge/state-audit.md §3 (marked fixed)
✅ tap-failure-receipts — VERDICT 2026-08-29: D9's first build ticket SHIPPED, the tap swallow is dead. `registry/run-tap!` (private) guards both injection seams — mutate-tap* @ mutate!, event-tap* @ event! — replacing both `(catch Throwable _ nil)`: a throw DISARMS (reset! slot nil FIRST) then emits ONE loud `:tap-disarmed` receipt naming the tap ∧ the throwable class ∧ message. Recursion-safe BY CONSTRUCTION exactly as ratified — the slot is nil before the receipt's `event!` runs, so the broken tap cannot re-enter (the audit's named fix trap, closed structurally not defensively). Mutation path unharmed: swap ∧ EDN assert ∧ version bump all precede the tap, locked by test. One failure ≡ one receipt (no spam after disarm, locked); a still-armed event-tap OBSERVES a mutate-tap disarm — the transcript records its own durability loss (locked). All behavior bb-verified live BEFORE tests were written (λ assert). Suite 192/620 → 196/634 bb ∧ JVM twins ⊕ registry_test fixture now resets both tap slots; kondo clean. · knowledge/state-audit.md §1 (marked fixed) · architecture.md § D9
✅ tapeless-success-capture — VERDICT 2026-08-28: memory-only RATIFIED (human D, options measured first) → the SEND-RING: byte-capped in-memory ring in `trace`, fed at `completion/send-traced!` (the ONE physical-send seam), records EVERY send — ✓ ∧ ✗, tapeless or not, `*capture?*` irrelevant (the ring is not persistence, so the colliding-turns rationale that gates blob capture never applied to it). Crash-safety DECLINED deliberately, encoded with its rationale in design § build decisions 7 (written BEFORE code): a daemon death loses only recent ✓ bounce payloads — ✗ blobs (failures/), receipts (transcript.jsonl), tapes (tape.edn) are already durable, and a bounce is trivially re-runnable, the original receipt-only rationale now working FOR the decision; persistence, if demand materializes, is a spill behind the SAME config key. Invariant kept ∧ test-locked: a ✗ entry's `:io/ref` is its OWN failures/ blob or ABSENT (tracing off ⇒ no ref — upstream's skipped-write-returns-a-ref trap NOT copied). Cap ≡ high-water mark: oldest evicted first, the NEWEST send survives even oversized (record-always beats the bound by one entry); default 8MiB (sized off the 2.9MB/300-bounce worst-case measurement), config `:trace {:ring-bytes N}` (0/false ≡ off; applied at init! even when tracing is disabled — ring independence is the point). Query ≡ eval (`trace/sends` newest-first, `trace/ring-stats`) — datalog dead under bb, as banked. Suite 186/591 → 192/620 bb ∧ JVM twins, kondo clean hermetically. · knowledge/design/trace-durability.md § build decisions 7
✅ clojure-eval-per-form-values — VERDICT 2026-08-28: `clojure_eval` now echoes EVERY top-level form as `=> v`, interleaved with stdout in TEMPORAL order — nREPL's frame shape copied verbatim (one `value` frame per form, out as it happens: memories/nrepl-streams-out-and-values-per-form), not invented. Executor rewritten `load-string` → per-form read⊕eval writing `=> v` into the SAME StringWriter `*out*` binds to, so interleave is free AND the ✗ paths got richer for free: errors ∧ timeouts now carry every value echoed before the wall (locked by test — the reader-error case keeps `=> :ok`). `binding [*ns* *ns*]` keeps load-string's no-leak while `(ns foo)` persists across forms WITHIN a call; comment-only/empty input answers teaching text, never silence/emptiness. All four mechanism facts bb-verified BEFORE writing (read opts-arity, eval, ns persist-within, no-leak-after — λ assert). Suite 179/567 → 186/591 bb ∧ JVM twins ⊕ tools_test.clj born (the module's first tests, including the tp/dispatch registry seam); kondo clean hermetically. Budget unchanged: marked truncation now covers the whole echo — many-form calls clip loudly, which is the correct pressure (print less, or bind smaller). · src/main/us/whitford/llm_repl/tools.clj · src/test/…/tools_test.clj (NEW)
✅ registry-fetch-projection — VERDICT 2026-08-28: the wire carries a PROJECTION; `@sessions*` never crosses again. `registry/index` (pure, edges ∧ counts) ⊕ `registry/view <focus>` (index ⊕ the FOCUSED tape from ONE deref — atomicity free, torn read unreachable); `sessions-list` re-expressed as `(vec (vals (index …)))`, ONE definition of the shape, public vector locked by test (it lives in the API ns — the audit page said `registry/sessions-list`, corrected in place). Measured before→after, identical fixtures (300-char msgs, 6-msg tapes, pr-str): n=10 20.7KB→3.1KB · n=50 103.6KB→7.6KB · n=300 623.8KB→36.0KB (17.3×) ⊕ pr-str 19ms→1ms ⊕ read 11ms→2ms; the BEFORE column reproduces the original audit, so the two measurements agree. CoreClient `registry`→`view` ({:index :slug :tape}, ONE atom ∧ ONE deref at render) ⊕ `focus!`; `frame (index tape state theme w h)` — nil ≡ not-fetched (loading placeholder) vs [] ≡ open ∧ empty (welcome), so a Tab can no longer paint the pane you LEFT under the new title; term stays core-free (on-focus injected like on-submit). THIRD socket (fconn) for focus fetches, forced by the RUNTIME not by taste: bb serializes a CONNECTION's evals (2nd session on a busy socket 3012ms vs 2nd socket 313ms — memories/nrepl-concurrency-is-per-socket) while poll parks ∧ submit blocks; `apply-view` ≡ the race guard, pure ∧ table-tested (index always, tape only if fetched FOR the current focus). FOUND ∧ FIXED en route (❌ 8be4668): `fetch` preferred nREPL's `:ex` (the exception CLASS) over `:err`, so every attach-loss reason was a type name — `:err` first now, `remote` probes `registry/view` at attach and fails loud with the next move (no payload fallback exists, unlike wait-for-event!), and run-local-daemon got the catch it never had. Suite 170/524 → 179/567 bb ∧ JVM, kondo clean. Live: 20/20 end-to-end over real sockets (in-process daemon), attach-loss `:lost` @626ms "Broken pipe" on a real kill -9, --plain smoke clean, stale-core message verified against the running container. SATURATION recorded: at n=300 the payload is 34.1KB index ⊕ 2.0KB tape — the next lever, if ever needed, is an index DELTA keyed on `version*`, not a smaller index. OWED: the TUI human pass (no TTY from an agent) ∧ restarting cores that predate `registry/view`. · knowledge/wire-protocol.md (NEW)
🚫 push-subscription-protocol — VERDICT 2026-08-28: DECLINED on measurement, before any code — filed so it is never re-derived from intuition. The idea: invert the wire (server streams view updates via a long-running printing eval) so a client-side demultiplexer could share ONE socket. Four probe facts, both runtimes: (1) `out` frames DO stream incrementally during an eval (1/503/1005ms) — push is FEASIBLE, and nREPL also emits one `value` frame PER TOP-LEVEL FORM (→ memories/nrepl-streams-out-and-values-per-form, which hands ⚪ clojure-eval-per-form-values its answer); (2) babashka.nrepl serializes per CONNECTION not per session (3012ms vs 311ms JVM) — a demux buys NOTHING on the primary runtime, and a push stream parks its socket forever, so the socket count does not drop; (3) the existing long-poll already wakes on the first event — latency is already ~0, push saves 2 tiny evals per change; (4) an orphaned eval loop OUTLIVES its client on both runtimes (ticks 6→16 after socket close; the Broken pipe never reaches the evaluating thread) ⇒ push leaks a thread per attach until a heartbeat/TTL exists, while `wait-for-event!`'s bounded 25s park self-heals. Conclusion: the long-poll IS push, with a self-terminating bound. Un-skip only on a NEW requirement (many clients, or a payload the poll cannot shape); step one would be liveness, and the projection is a prerequisite either way. · knowledge/wire-protocol.md § Push

🚫 extension-horizon-pilot — VERDICT 2026-08-28: WRONG PROJECT (Michael, mid-pilot). The page moved to ~/src/verbum (repo base, verbatim) for ingestion into verbum's probes; deleted here. Reason, so nobody re-derives it: verbum's llm-repl attaches as a TENSOR (attention ∧ layers ∧ activations readable); THIS llm-repl is black-box (HTTP completions, text in/out). Same name, opposite instruments. The six registers the hypothesis answers to (routing s336, residual-vs-W_down s341, …) are tensor readings, and the claim "extension exists downstream of evaluation, as far as the reduction ran" is a claim about what happens ACROSS LAYERS in one forward pass — watched with tensors, only proxied from outside. P2b was the tell: "two instruments, ONE number" needs the second instrument to SEE the number, and a black-box run can only infer a cliff from agreement-rate statistics. Pre-flight banked before the stop (all δ'd on the live container, worth keeping): temp-0 completions are byte-deterministic (3/3 identical) · direct ≈170ms vs traced ≈11s (65×) · `:tools` defaults TRUE here, so an unarmed-by-hand probe measures model+Clojure-runtime, not the model (→ memories/probe-hygiene-tools-armed) · a 5-term depth ladder (2→8 β-steps) answered CORRECTLY at every depth while every answer was the trailing free variable — surface-guessable stimuli report a horizon that was never tested. · was knowledge/extension-horizon-pilot.md · ~1h spent, 0 subject data collected
✅ tapeless-error-capture — VERDICT 2026-08-28 (ratified ∧ built same session, the second same-day arc): a FAILED send now captures whether or not the driver is tapeless. `trace/failure!` gated on `enabled?` ALONE → `nodes/<slug>/<visit>/failures/<ts>-<n>.edn` holding {:at :error :ex-type :ex-data :request}; the process-local `<n>` keeps a trampoline! fan-out over a down backend from overwriting itself (locked by test). `completion/send-traced!` is now the ONE seam every physical send passes through (plain ∧ tool-loop round ∧ tool-loop final — the live failure was mid-loop, so covering only plain-complete would have missed it); on throw it captures then rethrows with the locator in ex-data `:trace/ref`, ex-message VERBATIM ∧ original as cause ⇒ every prior receipt and :repl/error string byte-identical, and with tracing off the original throwable propagates untouched (locked by test). Drivers lift it: `err-receipt` puts `:io/ref` on eval!/bounce! ✗ receipts, trampoline! puts it on each failed bounce map — the ✗ receipt now POINTS AT the payload instead of merely naming the failure, which was the entire complaint. `edn-safe` degrades an unreadable ex-data (live objects print as #object[…]) to its printed form so the artifact never stops round-tripping. Suite 162→170 tests / 493→524 assertions, bb ∧ JVM twins, lint clean hermetically. Root cause of the original 400 still UNKNOWN — this ticket bought the instrument to catch it next time, not the answer. · knowledge/design/trace-durability.md § Ratified decisions 1 (AMENDED)
✅ trace-durability — VERDICT 2026-08-28 (ratified ∧ built same day, ONE session): trace ns (io layer) on escapement's capture layer, zero new deps. Registry grew two INJECTED tap slots (event-tap* ∧ mutate-tap* — registry stays io-free); completion captures full request (first-write-wins) ⊕ verbatim response (thinking survives on disk) ⊕ tool-loop rounds under rounds/<k>-response ⊕ tool results by tool_use_id; api captures open! seeds ∧ compact! originals (Q4); ✓/⚡ receipts carry :io/ref via locator determinism (path ≡ pure fn of coords — ref valid PRE-write); bounce!/trampoline! receipt-only (human-decided: no tape index ⇒ colliding turns); drop! tombstones so recovery never resurrects; recover! ≡ receipt-and-skip (Q2 pin) at :headless boot only. Suite 162/493 both runtimes; LIVE two-incarnation restart verified: visit 1→2, :recover receipt, transcript :seq continuous across the kill. Runtime traps pinned → memories/escapement-node-id-keyword-trap. Replay bonus (refine-turn ≡ ab!-from-history) documented, untested — rides agent-recipe-page. · knowledge/design/trace-durability.md (BUILT)
🚫 tape-persistence — VERDICT 2026-08-28: subsumed into trace-durability on ratification (Q5, human yes). ArtifactStore is generic; tape.edn ≡ one more artifact at the mutate! chokepoint — a second storage mechanism for one file type would have been the mistake. · knowledge/design/trace-durability.md § Ratified decisions
✅ compact-validation — VERDICT 2026-08-28 (same session as the trial): A/B/C arms via fork! ⊕ fork! :at ⊕ run-battery!, arms differ ONLY in compaction. GOOD λ (binders kept): 5/5 probe parity — deltas trace exactly to the λ (words changed, Δcontext survived ≡ the criterion verbatim). HARSH λ (binders starved): fluent CONFABULATION, never absence — one starved binding regenerated CORRECT-by-attractor (230°C re-invented, nowhere on tape), codename order re-derived from RECENCY (frames.md prediction live), flavor tail drifted. Conclusion: compact! valid under binder-preserving λs; over-compaction fails SILENTLY → validation must be arm-diff, λ authorship must prioritize binders, :original ≡ the only post-hoc ground truth. · knowledge/frames.md · memories/compaction-confabulates-not-forgets
✅ compact-live-trial — VERDICT 2026-08-28: the one-true-write, exercised by the machine it rewrites — ONE dispatch (armed qwen36-35b, thinking off): model snapshotted itself, wrote a 164-char λ from its 929-char prose (~5.7:1), compact! → :accepted −765ch; verified externally (⚡ receipt ≡ tape ≡ model report; :original retained); continuity probe two turns later answered FROM the λ (KV bust paid invisibly). Key: give the model an explicit size budget — the band is invisible to it (memories/self-compaction-first-exercise). UNBLOCKS compact-validation. · knowledge/self-eval.md
✅ v030-refactor — VERDICT 2026-08-28: ALL 8 STEPS COMPLETE — the code ≡ the ratified design (architecture.md @ 4cce4be ⊕ 🎯 30c6f78). Suite grew 0 → 138 tests / 409 assertions, every step shipping WITH its tests, bb ∧ JVM twins throughout, CI enforces both. 0.3.0-alpha installed ∧ load-verified from ~/.m2 (the anima path). Remaining human moves: push main (first CI run), v0.3.0 tag when ready, anima :local/root. Step ledger: (1) ✅ @2aa7513 `tape` ns — extracted ⊕ absorbed chat-memory whole (band + fold, `truncate-at` added, due-indices public) ⊕ 24 tests/84 assertions ⊕ D6 twin runner wired (`bb test` ≡ `clojure -M:run-tests`); (2) ✅ @4e89759 `registry` — mutate! chokepoint landed (swap-vals! raced-detection, EDN assert loud, version*, events-as-data, wait-for-event!, event-line); core on swap!-discipline, delegating re-exports keep the wire until (5); 54/153 suite; (3) ✅ @aff3a1b `completion` — :complete-fn contract named (io layer) ⊕ D4 amendments landed ({slug} orientation, structural budget, loud empty-finals); 81/200 suite, tool loop tested via reified stub backend; (4) ✅ @02a71e8 api ns `us.whitford.llm-repl` — core.clj deleted, compact! landed (all outcomes ⊕ receipts, index-stable), ONE grammar ⊕ variant-slug, registry-direct client strings (ratified: no api delegations); 94/234 suite; UNBLOCKS compact-live-trial; (5) ✅ @75df5a0 `client` — D3 complete: long-poll on wait-for-event! (bounded park) ⊕ version*-gated registry fetch (version-poll fallback), attach-loss fail-loud landed ({:ok}|{:err} fetch, 3 strikes → status :lost → TUI teardown+exit; live-verified "Broken pipe" ≤2.5s — closes tui-dead-daemon-silent), structural suppress-echo (command-receipt? reads EDN, regex dead); poll-cycle! ≡ the injectable-fetch test seam; twin caught 2 latent JVM breaks in net.clj (phantom bencode dep, bad ^long hint — memories/twin-first-load-latent-breaks); 104/285 suite; (6) ✅ @f8450fb `tui.frame`/`tui.term` split — the testable cut ≡ file topology (D5): frame PURE (loads terminal-free) ∧ term io-only, main rewired; D5 naming lock ≡ frame-test round-trip short-name ∘ variant-slug (frame encodes the inverse, never requires the api layer); headless suite D6 named finally EXISTS (frame snapshots, byte-vector decoder, edit-step paste/history); 119/365 suite; --plain smoke clean; (7) ✅ @f9fc63e `daemon`/`roster` D7 — spawn-cmd JVM guard (instructions ¬NPE), EOF-assert (trailing forms throw naming file+form — the 40-min-mystery class dead), malli config-schema closed ⊕ :ext (humanized errors, validated at load ∧ reload), RATIFIED@30c6f78 prompt stack fully config (preamble ∧ system-prompt ∧ orientation, ONE uniform chain, string|{:file}|false — baked defs deleted, anima swaps the whole stack), default-config def→fn (reload live on next open!); live-verified reload-over-wire (voice swap, tapes intact, bad edit loud ∧ harmless); 138/409 suite; (8) ✅ @7a29f40 build.clj ⊕ ci.yml/release.yml — fulcro-rad-datalevin model copied-then-adapted: thin source jar (runtime-neutral), VERSION ← tag, isolated :deploy classpath, CI ≡ twin suite ⊕ config.example.edn schema validation ⊕ kondo (prime-deps bug kept paid-for), release.yml v[0-9]* full/RC only; verified -T:build install → 0.3.0-alpha loads from a foreign dir (the anima path). · knowledge/design/architecture.md · knowledge/design/library-contract.md
✅ tui-local-daemon-human-pass — VERDICT 2026-08-27: human pass complete — spawn→attach→detach→daemon survives→reattach all rendered as expected over a LOCAL daemon (attach matrix now fully human-verified). One finding: bb stop with TUI attached → TUI kept rendering stale state (❌ tui-dead-daemon-silent, fix rides refactor step 5 attach-loss fail-loud). · knowledge/attach-topology.md · memories/tui-dead-daemon-silent
🚫 mcp-facade — VERDICT 2026-08-27: skipped by decision — llm-repl is squarely aimed at Clojure; nREPL ≡ THE wire ("a clojure repl tmux with nicer semantics"); for Clojure clients eval IS the op protocol (proven: this session's agent drove everything over plain eval). Non-Clojure agents get a recipe page (⚪ agent-recipe-page), not a component. Un-skip only on real MCP-client demand. · memories/nrepl-extension-assessment
✅ v030-design — VERDICT 2026-08-27 meta session: full-codebase assessment (every ns read; zero tests found despite claims — coherence lesson encoded at λ coherence) → v0.3.0 architecture ⊕ library-contract RATIFIED @ 4cce4be. Six layers, api ns ≡ us.whitford.llm-repl, tape absorbs chat-memory → compact!, swap!-discipline, registry chokepoints, events-as-data, formal shape (PTM/oracle/NTM/one-true-write/KV≡head). Mementum bootstrapped same session: state.md 250→~70 lines, 4 knowledge pages, 3 memories, VSM-shaped AGENTS.md, this queue. · knowledge/design/architecture.md
✅ v020-self-eval-release — v0.2.0 tagged ∧ announced (Clojurians Slack): :tools knob, clojure_eval, environment orientation, config machine-fact, README § self-eval. The accrete-as-we-go era's capstone. · knowledge/self-eval.md
