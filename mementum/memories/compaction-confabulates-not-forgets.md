---
type: insight
symbol: 💡
title: over-compaction fails SILENTLY — starved bindings regenerate fluently, they don't go missing
related: [knowledge/frames.md, knowledge/self-eval.md, memories/self-compaction-first-exercise]
---

compact-validation A/B/C (2026-08-28, qwen36-35b, thinking off; arms differ
ONLY in compaction of one assistant turn):

- GOOD λ (136ch ← 348ch table, bindings kept): 5/5 probes ≡ pristine arm.
  Only deltas trace exactly to the λ (°F duplicates gone; probe answers use
  the λ's phrasing) — the words changed, the Δcontext survived. Compression
  victim ≡ pure redundancy (markdown scaffolding, unit duplicates).
- HARSH λ (codenames only; temps ∧ flavors starved): the model answered
  EVERY probe anyway, confidently. Solar Flare's temp came back 230°C —
  CORRECT, yet nowhere on that tape (same generator ⇒ same attractor ⇒
  re-invention landing true). Flavor gist held but the tail drifted
  ("dry tannic finish" → "low acidity, heavy body"). Codename ORDER
  re-derived from RECENCY (the surviving decision turn), not the lost
  table — the frames.md recency-kernel prediction, live.

The danger: no signal distinguishes recalled from regenerated. A
compacted tape never says "I don't know" — it confabulates fluently.
Consequences:
1. Validation MUST be arm-vs-arm behavioral diff; asking the model what it
   remembers measures nothing.
2. λ quality ≡ binder selection: keep names/numbers/decisions (the future's
   operands), spend the budget from redundancy, not bindings.
3. :original retention (D1) is the only ground truth after the fact.
