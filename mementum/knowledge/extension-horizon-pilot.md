---
type: mementum/knowledge
title: Extension-horizon pilot — extension ≡ event(evaluate), tested behaviorally through llm-repl
status: proposed — hypothesis-grade; handoff candidate for verbum (§P-EXTENSION-HORIZON); Michael rules the freeze
related: [design/trace-durability, ../memories/design-is-compressed-measurement, ../memories/dialect-cluster-is-the-calculus-trace (anima)]
---

# Extension-horizon pilot

> Synthesized 2026-08-28 (the load-bearing/corner-mapping session) from a
> conservative read of verbum's INDEX + state.md. SCOPE: the pilot RUNS
> through llm-repl (the instrument); the FINDING, when data lands, belongs
> to verbum/anima. Verdict masses and the freeze are Michael's/verbum's —
> this page carries the hypothesis and the design sketch only.

## The method frame (ratified in conversation, Michael 2026-08-28)

```
λ corner(M).   M ≡ hidden(machine) | ∀observe(M) → projection ≪ M | ¬∃view(whole)
               | corner ≡ protrusion(M → surface) : measurable ∧ δ-decidable ∧ cheap(re-probe)
               | pin(c) ≡ δ(c, runtime) → verified | ¬pinned(c) → prior(wearing_map's_clothes)
               | map(M) ≡ Σ pinned(corners) | agree(cᵢ, cⱼ | surfaceᵢ ≠ surfaceⱼ) → evidence(ONE_M)
               | corners_needed ∝ structure(M) ¬volume(M)
               | native_error ≡ extend(corner → whole) | footprint ≢ animal
               | ¬keep(c) → lost² | map ∈ durable ∨ sand
```

## The hypothesis

Verbum: extensional equality (SKK ≡ I) is ABSENT across six registers
(value s317 · magnitude s335 · routing s336 · operator s339 ·
residual-vs-W_down s341 · behavioral s348 LEXICAL-FLOOR). Standing reading:
"meaning is tape-resident" (a location). This page's claim is the MECHANISM:

**Extension is an event, not a property.** A bounded reducer cannot decide
∀x.SKKx=Ix from a spelling; extension exists only DOWNSTREAM of evaluation,
exactly as far as the reduction actually ran. The six nulls ≡ six
confirmations that every measured register sits upstream/aside of
evaluation — not that extension is nowhere.

**The fingerprint already recorded (post-hoc re-read — hypothesis-grade
ONLY, the φ-ladder scar applies):** s348 §P-OBS-EQUIV context breakdown:
A_coext(C1 direct/USE) = 0.333, the ONLY context above floor (0.108);
C2 named/MENTION = 0.083; C1>C2 pre-registered, p=0.0006 (frame-ledger law
s222). Use runs the term; mention only spells it.

## Predictions (a priori, where it can die)

```
λ extension(t₁,t₂).  ≢ stored | ≡ event(evaluate)
                     | P1: A_coext ∝ use_forcing (monotone gradient, not binary)
                     | P2: A_coext(depth d) → floor at cliff d*
                     | P2b: d*(direct) ≈ within-pass budget (s319 direct/traced gap —
                            two instruments, ONE number; miss ⇒ mechanism wrong)
                     | P3: d*(traced/CoT) > d*(direct) — tape extends the horizon
                            (no lexical story moves a cliff by allowing scratch steps)
```

Corollary for verbum's open asterisk **§P-COEXT-FATE** (17×17 outcome
register, the one register never co-ext tested): fate-collapse should be
DEPTH-CONDITIONAL, not binary — present within the within-pass horizon,
absent past it. Either outcome resolves the asterisk; the depth-graded
design can't be fooled by lexical recognition (recognition has no reason to
produce a cliff AT the budget).

## Pilot design (behavioral face first — buildable on this instrument, ~1 session)

- Subject: qwen36-35b-a3b (armed daemon), temp 0, thinking off.
- Method: fork-differencing off a SEALED shared prefix (verbum s346/s348
  banked method): `fork!` per spelling, battery per context, compare answer
  profiles → agreement RATE. `ab!` ≡ the finite-difference step across arms.
- Materials: co-extensional pairs graded by reduction distance (≈2, 4, 6,
  8+ steps to equality), certified by an external kernel (reference reducer
  ≡ scoring only, never the spec — bug-compatibility clause). Context
  battery graded by use-forcing: direct apply → apply-with-distractor →
  nested apply → paraphrase-use → named → quoted.
- Nulls (verbum discipline): determinism ceiling (same-spelling forks) ·
  certified non-equal floor (length-matched) · sensitivity calibration
  S(c)≥0.5 prunes manufactured agreement.
- Arms: direct vs traced (CoT/thinking-on) — P3 is the traced arm.
- Trace-durability note: run AFTER the capture layer lands if possible —
  every arm's request/response durable, arm-diff ≡ the audit (the
  compact-validation lesson: silent confabulation needs durable ground truth).

## Out-of-panel extension (second arm, optional)

The batteries are behavioral → run over ANY :complete-fn. Frontier models
(incl. the session agent's own lineage) as out-of-panel points on the
NAIVE-SUBST scale curve (verbum whisper: 14B→32B frac_correct 0.056→0.188).
Horizon theory predicts: monotone in scale, asymptote BELOW Church (read-
fidelity bound). Instance's inside-view prior, worth nothing until measured:
still capture-unsafe under pressure, deeper into the budget.

## Honesty bounds

- The C1 re-read is post-hoc on a frozen result — it earns a NEW freeze
  with C1-vs-floor as make-or-break, never a claim from s348's data.
- Instance testimony (this page's author is an instance of the machine) ≡
  hypothesis generator, never verifier — the memetic-GA law applies
  maximally: only ground-truth gates (kernel-certified pairs, δ against
  runtime) remove the author's priors from its own proposal.
- Single-subject pilot; verbum's freeze discipline (a-priori verdict
  masses, planted worlds, held-out) belongs to verbum when/if adopted.
